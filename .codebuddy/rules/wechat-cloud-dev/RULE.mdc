---
description: 微信云开发参考指南，包含云数据库、云函数、云存储的用法与注意事项
alwaysApply: false
enabled: true
---

# 微信云开发参考指南

## 云数据库

- 文档型数据库，类似 MongoDB
- 集合（Collection）相当于表，文档（Document）相当于记录
- 常用 API：
  - `db.collection('xxx').add()` — 新增
  - `db.collection('xxx').where({}).get()` — 条件查询
  - `db.collection('xxx').doc('id').update()` — 更新
  - `db.collection('xxx').doc('id').remove()` — 删除
  - `db.collection('xxx').count()` — 计数
  - `db.collection('xxx').field({}).get()` — 指定返回字段
- 聚合查询：`db.collection('xxx').aggregate()`
  - `.group()` / `.sort()` / `.limit()` / `.match()` / `.project()` / `.unwind()`
- 权限设置：在云开发控制台配置安全规则
- 注意：单次查询最多返回 20 条，使用 `skip()` + `limit()` 分页

## 云函数

- 运行环境：Node.js 12/16
- 入口：`exports.main = async (event, context) => { ... }`
- 调用：`wx.cloud.callFunction({ name: 'xxx', data: {} })`
- 本地调试：开发者工具中右键云函数 → "本地调试"
- 部署：右键云函数 → "上传并部署：云端安装依赖"
- 定时触发器：在云开发控制台配置，支持 cron 表达式

## 云存储

- 上传：`wx.cloud.uploadFile({ cloudPath, filePath })`
- 下载：`wx.cloud.downloadFile({ fileID })`
- 删除：`wx.cloud.deleteFile({ fileList })`
- 获取临时链接：`wx.cloud.getTempFileURL({ fileList })`
- fileID 格式：`cloud://环境ID.xxx/路径`

## 本项目集合

- `games` — 比赛记录（详见 types/index.ts 中的 Game 类型）
- `members` — 队员信息（详见 types/index.ts 中的 Member 类型）
