---
description: 星羽炮分榜小程序编码规范与开发约束
alwaysApply: true
enabled: true
---

# 星羽炮分榜编码规范

## 语言与框架

- 使用 TypeScript 开发，禁止使用纯 JavaScript
- 微信小程序原生框架，不使用第三方 UI 框架（如 Taro、uni-app）
- 云函数使用 Node.js，禁止引入额外运行时

## 代码风格

- 缩进：2 空格
- 命名：文件名用 kebab-case，变量/函数用 camelCase，类型/接口用 PascalCase
- 页面目录结构：每个页面一个文件夹，包含 `.ts`、`.wxml`、`.wxss`、`.json` 四件套
- WXML 中使用 `wx:` 指令，不使用自定义指令语法

## 小程序开发约束

- 不使用 npm 包，除非必须（优先用小程序原生 API）
- 数据存储只用微信云数据库，不引入其他数据库方案
- 图片资源放 `miniprogram/images/`，音效放 `miniprogram/static/audio/`
- 云函数必须放在 `miniprogram/cloudfunctions/` 目录下
- 页面路由在 `app.json` 中注册

## 业务逻辑约束

- 炮分计算必须遵循四因子公式，不可随意简化
- 对阵生成算法（match-engine.ts）是核心逻辑，修改需谨慎
- 玩法配置（play-modes.ts）中的参数有业务含义，不可随意调整默认值
- 云数据库操作注意权限设置（安全规则）

## 文案风格

- 空状态、错误提示等文案保持"炮哥"IP 人设风格（硬核、有趣、带"炮"元素）
- 不使用过于正式或书面化的表述
- 示例：空状态用"还没有炮局，快让章一炮开一炮！"，错误用"哑炮了，再试一次"
